import { DateToJMYPipe } from './date-to-jmy.pipe';

describe('DateToJMYPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToJMYPipe();
    expect(pipe).toBeTruthy();
  });
});
